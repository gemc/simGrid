This is a placeholder directory that contains two files:

- a link to `../../clas12-config/setup.json` for the portal to display the available 
  configuration. T
- a osgLog.json for the portal to display the status of the ongoing jobs. This is 
  copied here by a cronjob that copies it from the submit node where it is created